export type Data = {
    id: string,
    tmdb_id: number;
    title: string;
    date: string;
};

export type DataForm = {
    id: string,
    tmdb_id: number;
    title: string;
    date: string;
};

export type User = {
    id: string;
    name: string;
    email: string;
    password: string;
};